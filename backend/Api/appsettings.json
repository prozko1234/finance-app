{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Push": {
    "_comment": "Charge reminders. Generate a pair with `npx web-push generate-vapid-keys` and set them here in development, or as Push__PublicKey / Push__PrivateKey environment variables in production. Empty keys mean reminders are off, and the app says so once at startup.",
    "PublicKey": "",
    "PrivateKey": "",
    "Subject": "mailto:bfilimonyuk@gmail.com"
  }
}
